pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "PlaybackTrainer"
include ':playback-trainer'
include ':playback-trainer:app'
include ':playback-trainer:core'
include ':playback-trainer:core:pitchdetector'
include ':playback-trainer:core:media'
